parameters:
    level: 8
    paths:
        - src/

    excludePaths:
        # Makes PHPStan crash
        - 'src/DependencyInjection/{{ pluginPascalCaseName|replace({'Plugin': 'Extension'}) }}.php'

        # Test dependencies
        - 'tests/Application/**/*'
